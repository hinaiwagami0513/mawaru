# raw: 2026年8月 作業ログ

> 生素材。**新しい日を上に足す**。既に書いた日のセクションは書き換えない（間違いは同じ日の下に訂正を足す）。
> このリポジトリは公開なので、**報酬・請求・契約形態と社外秘の議題は書かない**（[[raw/mtg-2026-08-21-iwagami]] と同じ方針）。
> 取引先への共有はこのファイルを原本にする。1日分は「やったこと / 見つけた不具合 / 次にやること」の3節で揃える。
> 収集コマンド: `tools/collect-worklog.sh 2026-08-22`（その日のコミットと差分統計を出す）

---

## 2026-08-30（日）

### やったこと

**キツネのセリフを×で閉じられるようにした（16画面 28個）**
- 据え置きのアラートは一度読んだ人にも毎回同じ文が居座る。ドライブの ATTENTION はSPで本文6行ぶんを占めファイル一覧を押し下げていたので、読み終わった人が自分で閉じられる逃げ道を1つ足した。既定は出したまま
- `iine_board/fox-alert.js` を新設。HTMLに×は書かず、`fox-alert.css` を読む画面に `<script>` を1行足すとその画面の全 `.fox-alert` に×が付く。×は吹き出しの中の右上に `float:right`（外に兄弟で置くと 28px + gap 12px が本文から引かれ、SPで吹き出しが 306→266px に痩せて1行増える）
- 保存は `localStorage`。鍵はページ名 + 本文のハッシュ（番号で持つと後から1つ足したときにずれて別の箱が消える）。`.err` は閉じるが覚えない（次も失敗していれば出るべきもの）
- 線引きは「キツネが話しかけているか」。数値の横の表情アイコン（`.summary-fox` / `.mc-fox` / `.trend-row`）とローディング・お祝いのキツネは対象外。箱を持たない一言（AIレポートのスライド下8個）は `.fox-say` を新設して同じ扱いにし、後から差し込まれる2つは `MutationObserver` で拾う
- アイコンは `ti-x` ではなくSVG（字形を持たない画面で空白の四角になるため）。`settings_wire` の手書きの×は削除。DESIGN.md §Alert を「中に float で浮かせる」に改訂

**SNS設定にアカウント情報を追加した**
- 旧UI（設定 > SNSアカウント > アカウント情報）にあって新UIに無かった機能。旧版は見出し11本・箇条書き100行超の文章の壁だったので、数字タイル4枚・構成比バー・ピル3枚に作り直した。バーは `--chart-1..5` のオレンジ単一色相ランプで、5を超えると破綻するので上位4 + その他に畳んだ
- 入れるのはAIの理解だけ（どんなアカウントか / いまの状態 / よく出しているもの / 投稿のかたち）。成績・改善案・目標は分析タブの担当なので末尾のリンクで送る。読むだけで編集ボタンは置かず、ズレていたら資料を足して「読み直す」を押す
- 投稿ルール（既定）/ アカウント情報の2タブに分けた。ルールの見出しの下に積んでいて、AIの読み取り結果がルールの1つに見えていたため。タブは既存の `.ptabs` を流用
- 「回っているかの目印」を足した。基準は同じ画面の「1週間に出す本数の目標」＝ユーザー自身が決めた数字。判定 → 根拠1文（「目標は週1本。直近8週のうち6週で出せています」）→ 8週ぶんのマス、の3段でヘルプ無しで読める形にした
- 地は状態色にしない（状態色の帯はキツネアラートと見分けが付かず、アラートの自作になる）。成熟度バッジ「育ちはじめ」は一度入れて外した（段階の定義が無く、隣の数値タイル以上のことを言っていない。開設からの月数は事実なので説明文に残した）
- 「メンバーを招く」ボタンに導線が無く押しても何も起きなかったので、`dlgGoal` と同じ自前ダイアログを追加。権限はネイティブ select ではなく `.picks` を1列で積む（招くときはまだ何を選べばいいか分かっていないので、3つの説明を並べて見比べられる形）。送ると一覧に「招待中」の行が増えて人数も動く

**投稿レポートの指標を各投稿にも出した**
- プロフィール率・シェア数・インタラクション・リーチしたアカウント数は全体と詳細シートには前からあり、抜けていたのは一覧カードだけだった（表示・いいね・コメントのみ）
- 一覧＝見比べる場所 / 詳細シート＝詳しく見る場所で切り分け、一覧は主役3つ（リーチ / 反応 / プロフィール率）に絞った。残りは押した先で見る
- 詳細シートは7つを同じ大きさで4列に並べていたのをやめ、「見られた（28px）」「反応された（21px → 内訳は本文サイズ）」の2組に分けて大きさで差を付けた。数字はオレンジで揃えた。4列をやめたことで「リーチしたアカウント数」が2行に折れる問題と最後の1マスが余る問題が消えた
- 一覧カードの数値を `posts` から描くようにした（`fillQuickStats()`）。HTML直書きで、目のアイコンにリーチ(127)を入れており詳細シート（目=表示203 / 人=リーチ127）と食い違っていた

**AIレポートの「出力」をタブからボタン + ダイアログに変えた**
- サブタブが 改善点 / 分析概要 / コンテンツ / 出力 の4つで出力だけタブだったため、横並びの「読む中身」に見えていた。サブタブを3つに戻し、更新日時の隣に「出力」ボタンを置いた
- 中身（画像 / PDF / テキスト）は押したあとのダイアログへ。3つから1つ選ぶので `alertDialog` は使えず、`settings_wire` と同じ `.dlgov` / `.dlg` を流用。ダイアログはタブを見ていて「〇〇 を出します」と出し、PNGの枚数もそのタブのスライド数にした（改善点5枚 / 分析概要4枚 / コンテンツ8枚）

**投稿詳細シートに 改善点 / 分析概要 / コンテンツ + 出力 を入れた**
- 旧UI（投稿データ画面の右下）の4つ組を新UIのシートに入れる依頼。旧版は番号付きの長文が縦に積まれた文章の壁だった（未コミット）
- 改善点＝番号 + 見出し + 「いま → こうする」の差分チップ + 説明。「ハッシュタグを5個から12〜15個に増やしましょう」と文で書くと読み飛ばされるので `5個 → 12〜15個` だけ抜いた（未コミット）
- 分析概要＝既存の `analysis` をそのまま。コンテンツ＝形式 / 画像枚数 / 本文の文字数 / ハッシュタグ数 + キャプション実物で、数字は `caption` から数える（データで二重に持つと文面を直したとき数字だけ古く残る）（未コミット）
- コンテンツのタイルは2列×2段。シートは幅400px前後なので4列にすると1マス85pxになり「フィード」が2行に折れる（未コミット）
- 出力はタブに混ぜずボタン。開いている投稿を出すので、ダイアログに日付を出して取り違えを防いだ（未コミット）

**そのほか**
- 画像投稿の編集画面（`post_editor_image_wire` の「画像台本」タブ先頭）から紫の「台本のタイプ」パネルを削除。作り終わった投稿を直す画面で切り替えられると、今ある構成が作り直される導線に見えるため。種類を選ぶのは作成フロー側の担当なので `flow_image_v3` / `flow_image_material` のパネルと `edSw()` は残した
- 発注相談の「決まっていなければ『わからない』で大丈夫です」を削除。選択肢に「わからない」が並んでいるので、同じことを文で言い直していた

### 直した不具合
- メンバー管理とAIレビューが表示されなくなっていた。SNS設定にタブを入れたとき `</div>` を1つ閉じ忘れ、ブラウザの自動補完で `#paSns` の閉じが後ろにずれて、2つのペインが非表示のSNS設定の中に入り込んでいた。閉じタグに `<!-- /#id -->` を添えて追えるようにした
- 招待後の人数更新で `.card-h .d` を引いたところ、上の承認カードの説明文を掴んで「5人」で上書きしていた。`#memCount` を付けて解決
- SNS設定のアカウントヘッダーがSPで重なっていた（既存）。アイコン46px + 名前 + 統計2つを1行に入れており `.tx` が32pxまで潰れて `@inucafe_one` が46px溢れていた。1行目=アイコンと名前 / 2行目=統計に折って解消
- そのSP指定を `.acchd` の基本ルールより前に置いたため `.acchd .tx{flex:1}` に後勝ちされ、`flex-basis` が `0%` に戻って効いていなかった（`@media` は詳細度を上げない。`analysis_visual` の `.vr-*` と同じ罠）
- 凡例の行がグリッド項目の既定 `min-width:auto` で縮まず、320pxで14px溢れていた（`.ai-cat{min-width:0}` で解消）。`ti-eye` の字形宣言が漏れて空白の四角になっていたのも修正（既存）
- AIレポートの出力ダイアログが390pxで高さ1367pxまで伸びて画面外に出ていた。`.out-row` の `.tx` に幅が無く説明文が2〜3文字の柱になっていたため。基準幅を `calc(100% - 57px)` にし、`.dlg` に `max-height` と本文スクロールを足した
- 投稿詳細シートの分析概要ペインに `.sheet-analysis-list` を直接付けたところ、その `display:flex` が `.an-pane{display:none}` に後勝ちして切り替えても出しっぱなしになっていた。`.an-ov` を入れ子にして解決（未コミット）
- コンテンツのタイルの数字に `--text-body-lg` を指定したが、このファイルに定義が無く継承の14pxで出ていた。定義済みの `--text-h2`（20px）に変更（ここでトークンを定義すると他の使用箇所まで一斉に太るため）（未コミット）

### 見つけた不具合（未修正）
- メンバー管理とAIレビューのSP崩れ。320pxで `.card-h`（33px溢れ）・`.role`（25px）・`.iconbtn`（70px）、AIレビューの `.seg`（65px）、430pxで `.ml`（424px）。HEADに戻しても同じで、今まではペインが隠れていて測れていなかっただけ
- 「このまま続けたときの見通し」の棒グラフが12ヶ月ぶんあり、320pxで29px溢れる（既存）。横スクロールにするかSPは直近6ヶ月に切るかで表示期間が変わる話なので保留
- 投稿レポートの一覧カードだけラベルが「反応」で、全体・詳細シートは「インタラクション」。呼び分けが1つ増えている

### 次にやること
- スマホ版の残りの崩れ潰し（8/28会議の最優先事項）
- 奥澤さんからの残りのレビュー指摘の反映
- ホームの「今の状況」アイコンの色数（8/21・8/28と2回指摘、結論未定）

## 2026-08-28（金）

### やったこと

**サイドバーの帯を単色にした（ワイヤー27本）**
- 縦グラデーション（`#fe7235`→`#e85513`→`#d54101`）をやめてベタの `primary-deep(#d54101)` に統一。SPではサイドバーが高さ60pxの帯になり、180度ぶんの階調がその中に圧縮されて帯状のムラに見え、古く見える原因になっていた（未コミット）
- 白文字のコントラストは4.58でAAを満たすので、文字色の扱いは変えていない（未コミット）

**分析5画面のスマホ表示を直した**
- アカウント帯がアイコン／名前／@ハンドル／数値／切替を1つのflexに入れており、`.acct-row` が切替(94px)の残り幅しか取れず4段に崩れていた。`.acct-row` を `display:contents` で透明にして「1段目=アイコン・名前・ハンドル＋右端に切替／2段目=フォロワー・投稿数」の2段に組み直した。折り返す行に `margin-left:auto` は使わない（残り幅を食って次行に押し出す）（未コミット）
- `analysis_report` だけ `.acct-hd` が既存CSSで `column` になっており、`flex-basis:100%` が高さに効いて数値チップが168pxまで縦に伸びていたので `flex-direction:row` を明示（未コミット）
- タブ帯が `overflow-x:visible` のままカードの縁で切れて「投稿レポート」が「投」になっていたので横スクロール化し、右端28pxをマスクでぼかして続きがあると分かる形にした。`.content` が縦並びflexなのでスクロール枠にすると帯が10pxまで潰れるため `flex:none` を併記（未コミット）
- サブタブは3つで350px。左右余白を18→6px（320px幅では2px）に詰め、`flex:1 1 auto` で余りだけ分け合ってスクロールなしで全部見せる。帯の左右marginも32→16pxにして本文と揃えた（未コミット）
- ページ名が「分 / 析」に割れていた（`.hdr-right` が277px占有）ので、状態表示のプラン名をSPで隠して場所を作った。`.app` の左右padding 10pxも `.content` の16pxと二重に効いていたので落とした。分析系は `shell-sp.css` を読んでいないので各ファイルに個別で持たせた（未コミット）
- カード見出しが2行に伸び、棒グラフの数値ラベル（棒の上に30px突き出る）と重なって読めなくなっていたので、見出しを全幅に折り返して衝突を解消。320px幅では棒5本が9px溢れるので棒の太さは変えず `gap` を8→5pxにした（未コミット）

**作業ログの自動生成を作り直した**
- 8/25〜8/28がまるごと欠けていた。macOS の `StartCalendarInterval` は発火時刻に電源が入っていない日を取り返さず、毎晩20〜21時に落としていたため23:00のジョブが一度も走っていなかった（`launchctl print` の `runs=0` で確認）（未コミット）
- ログイン時に走る追いかけジョブ（`RunAtLoad` + `--backfill`）を足して2本立てにした。mdに無い過去の日を古い順に埋める。下限は自動化初日の `2026-08-22`（未コミット）
- 昨日ぶんだけはmdにあっても1回書き直す（23時前に一度書いた日が途中の状態で固まるため）。1日1回に抑える印を `~/Library/Logs/.iineai-worklog-refreshed` に持たせた（未コミット）
- `insert-worklog.py` が常に先頭へ差し込む作りで、バックフィルすると順番が壊れた（8/26 → 8/25 → 8/27）。日付降順の正しい位置に入れるよう変更（未コミット）
- スプレッドシートの `view` タブはB1の日付を `log` から FILTER で引くだけの表にした。最新日を自動で拾う式にはしない（特定の日を指定して見たいため）（未コミット）

**8/28会議の内容を vault に反映（新規3ページ + 既存7ページ更新）**
- サービスサイトの制作をペンディングに変更。サービス名とメインカラーが変わる可能性があるため、先に本体のスマホ対応・機能修正を完了させる（未コミット）
- **色はセマンティック名でしか参照しない**（`#fe7235` 等をワイヤー・コードに直書きしない）を `design-tokens` に明記。メインカラーが変わってもトークン差し替えだけで済ませるため（未コミット）
- 狐キャラとロゴは名前確定まで作り込まない（現行の狐はお腹に「いいねAI」の文字入りで作り直しになる）（未コミット）
- SNS設定の全SNS一括変更（押すと全SNSに同じ内容を反映、あとから個別調整を重ねる想定）と、言葉遣い・推奨ワードの追加を `settings-page` に記録（未コミット）
- AIレポートの推奨アクションをホームへ移す方針と、初回オンボーディングを各 entity に記録（未コミット）
- 8/27に書いたエディタ面の `editor-sp.css` 集約と素材タブの折り返し修正をコミットで確定（内容は8/27の項）

### 見つけた不具合（未修正）
- ホームは推奨アクションを移した結果、文字だらけで詰まっている。移動自体は入り口で「今月はこれを意識する」が見える形になったが、ホーム側の整理は手つかず
- ホームの「今の状況」の色数が多い件が8/21に続いて再指摘された。ステータスの色分けは必要という立場と噛み合っておらず結論が出ていないので、8/21の「色を落とす」を確定として扱わず未決のまま持ち越した
- 作業ログはAIボードで動いたぶんしか取れない。Figma側の作業は記録されない

### 次にやること
- サービスサイトとキャラクター・ロゴは着手しない。本体のスマホ対応と機能修正を先に終わらせる
- 手で書いた日を自動生成に上書きさせたくないときは、`~/Library/Logs/.iineai-worklog-refreshed` にその日付を先に入れておく必要がある

## 2026-08-27（木）

### やったこと

**投稿編集（エディタ面）のスマホ表示を直す — ワイヤー8本 + 共通CSS 1本**
- 幅390pxでフロー4種と投稿編集の全ステップ44面、編集タブ14面、オーバーレイ22面を機械計測し、「親の `overflow:hidden` で切り落とされている要素」と「テキストが1行3文字以下に潰れている要素」を洗い出した。崩れは全部「投稿を編集」の面に集まっていて、前段のステップ（SNS選択・作り方・素材・一括確認）は既に無事だった
- 根本は3カラム（左240px プレビュー / 中央1fr 台本・投稿文 / 右auto AIパネル）が390pxで畳まれていないこと。`flow_video_script` と `flow_manual` にはこの畳みルールが無く、中央カラムが36pxまで潰れて編集タブと台本テーブルが親に切り落とされ、AIパネルは280pxのまま画面外（right=580px）に出ていた
- インラインstyleに依存した `[style*="grid-template-columns:240px"]` の当て方をやめ、`.ed-head` / `.ed-cols` / `.ed-tabs` / `.ed-panehd` / `.ed-typerow` のクラスを6ファイルに付与。SP対応は `editor-sp.css` 1本に集約し、各ファイルに散っていた重複ルールを消した（`shell-sp.css` / `topbar-sp.css` と同じ「レイアウト型ごとに1ファイル」の形）
- ヘッダー行はタイトル左のSNS種別アイコンをSPで外して、タイトルに1行を丸ごと渡した。「仮予約」14px・「自動保存ずみ」44pxで縦一列になっていたのが、仮予約（左）＋自動保存ずみ（右）の1行に収まる。種別はトップバーのタグとステッパーで分かる
- 台本テーブルは列に下限（96 / 210 / 150px）を与えて横スクロールにした（`shell-sp.css` の「表は列を潰さず横スクロール」と同じ判断）。編集タブは左右の余白を20→14pxに詰めて3つを336pxに収めた
- 仮予約ダイアログのフッターは4ボタンで350pxを分け合って「や / め / る」になっていた。「未定に戻す」に1行、「やめる・仮予約する・本予約する」に1行を渡し、仮と本を並べて比較できる形にした

**動画エディタ（ダーク）のSPが実質使えていなかったのを直す**
- `.tools`（ツールレール）と `.left-props`（プロパティ）が `position:absolute` のまま幅だけ100%にしてあり、ステージとタイムラインの上に重なっていた。プレビューもトラックも見えず黒い空白が出るだけの状態。position から降ろしてDOM順（ツール → プロパティ → ステージ → AI → タイムライン）に縦積みした
- タイムラインの目盛りは36秒を幅いっぱいに割る作りで、390pxだと2秒ごとの間隔が18pxになり「10s」以降（幅22px）が隣と重なって読めない。`.ruler .mk:nth-child(even)` を隠して4秒ごとにした（位置はJSがpxで持つので触らない）
- トラック表示が102pxで6本のうち2本しか見えなかったので `.tl` を180→240pxにして3本ぶん見せる
- パネル幅を引っぱるグリップ（`.props-resize` / `.resize-handle`）は1カラムでは掴む先が無いのに画面外（x=397）に残っていたので隠した

**画像エディタ（ダーク）と素材アップロード面の細部**
- ヘッダーの作品名を省略表示（`flex:1` + ellipsis）にして「保存して投稿へ」を1行に。`video_editor_dark` で既にやっていた判断を揃えた
- 「この画像の台本 / 1 / 4枚目 / タップで開く」は `white-space:nowrap` が無く見出しだけが min-content まで縮んで「この画像の台 / 本」に割れていた。3つ合わせて325pxなので nowrap で1行に収まる
- 素材の入口タブ「PCからアップロード / いいねドライブから選択」が padding 20px + body で2行に割れていたので余白と字を詰めた（`flow_image_material` / `flow_video_material`）
- ラベルピル `.tag2`「おすすめ」が行の余りを分け合って42pxまで縮んでいたので縮ませないようにした（`post_editor_image`）

### 見つけた不具合

- `post_editor_wire.html`（自動保存ずみ・シーン構成が縦積み）と `base_chat2_wire.html`（トップバーのタグが2行）にも同じ崩れが残っている。どちらも `board.html` に登録が無い旧版なので触っていない（現行は `post_editor_image_wire` と `chat_wire`）
- `video_editor_dark` の変化ポイント（`.kf`）は親クリップの下端から7px出て切れる。PC幅でも同じで `bottom:-2px` の設計どおりなので直していない
- 検出を組む途中で2回誤った判断をした。①要素の高さ ÷ line-height で行数を数えるとボタンの上下 padding を1行と誤って数える（Range の `getClientRects()` で実行数を数える形に直した）。②dev-server が落ちた状態で計測すると全ページが `chrome-error` になり「崩れ0」に見える（結果にページタイトルと要素数を含めて検証できるようにした）

### 次にやること

- 計測したのは幅390px（iPhone 14 Pro相当）だけ。360px（Android小型）と430pxの境界そのものは確認していない
- 崩れの検出スクリプトは使い捨てで作った。繰り返し測るなら `tools/` に置く形にしたい

---

## 2026-08-26（水）

### やったこと

**画面ごとの使い方案内（`tour.js`）を新設 — 23画面ぶんの手順を定義**
- 初めて開いた画面だけ、押すべきところを残して他を暗くし、吹き出しで説明する。既定の送りは「次へ」ではなく本物のクリック（手を動かした操作しか残らないため）
- 暗幕は4枚に割って穴の部分に要素を置かない形にした。`box-shadow` で1枚に抜くと穴もクリックを吸って「押したら進む」が成立しないため
- スキップ / Esc / 今後出さない、を用意し初回のみ表示。`onboarding.js` のダイアログが出ている間は待つ（暗幕の二重掛けを避ける）

**AIレポートを4ブロックに組み直し**
- 総合／企画／投稿文／コンテンツ／投稿日時 → 改善点／分析概要／コンテンツ／出力に変更。既存17枚のスライドは捨てずに割り振り、出力（保存・書き出し）を新設
- 改善点を先頭に置いた。レポートを開いた人がまず知りたいのは数字ではなく「で、何をすればいいのか」なので行動から見せる
- 出力タブはスライドを持たないので矢印とドットを隠す

**モバイル表示の追い込み**
- 分析5画面: `.app` の grid が SP でも 235px の列を残し本文が125pxに潰れていた。grid は `display:none` の要素を外しても列そのものは消えないため、シェル定義より後ろに打ち消しを置いた
- はじめの設定の箱: 60pxの下部ナビで幅を110px奪われラベルが重なっていたので SP では隠す
- ロゴ帯: クラス名が `.sbtop` と `.sb-logo` の2種類あり、後者が帯に残っていたのを統一
- 動画エディタ: ヘッダー右の3ボタンが402px必要で溢れていたため SP はアイコンのみに
- `board.html`: 上の帯が723px必要でページ全体が横スクロールしていたので折り返しを許可
- 390px / 375px × 25画面を計測して全画面OK、デスクトップ1440pxで回帰なしを確認

### 直した不具合
- 分析5画面のヘッダーにハンドラの無い `.close-btn` があり、押しても何も起きなかったので削除

## 2026-08-25（火）

### やったこと

**はじめの設定（チュートリアル）を新設 — `onboarding.js` を各画面で1行読み込む形**
- タスクは粗い4つに絞った（3ステップの完了率72%に対し7ステップは16%まで落ちるため）。入力欄ごとに割ると一覧が作業メモになるので、欄の細かさは飛んだ先の画面に持たせた
- 読ませるのではなくやらせる方針。練習用の画面は作らず本物のカードへ飛ばし、飛んだ先で対象カードを光らせる（`pjGo` を流用）。最初に出すのは1枚だけで、ステップごとの吹き出しツアーは作らない
- 完了条件は「画面を開いた」ではなく「フローを1回通した」に置いた。運用コンセプト=4項目すべて保存（行内に 2/4 を出して一覧は粗いまま進み具合が見える）／SNS=アカウントの中まで入る／投稿=完了モーダルに到達／動画編集=素材ルート経由でエディタに入り書き出しを押す
- 最後の1つで祝うダイアログを出して締める（トーストは出さない。二重になる）。閉じるとサイドバーのウィジェットも下げ、celebrated フラグで一度だけにした
- 状態は localStorage に持たせた（ワイヤーはページを跨ぐと変数が消えるため）

**運用のコンセプト4項目を `settings_wire` に追加（必須）**
- SNSで増やしたいもの（来店/予約/認知/採用の単一選択）／お客様の悩み／いちばん推すこと／他店との決定的な違い
- 事実（資料から取れる）とコンセプト（人が決める）を面内タブで分けた。1枚に積むと2233px（可視1100px）でコンセプトが下に沈むため、分けて 1569px / 1112px に
- タブに残り件数を出した（開かないと未入力に気づけない）。必須の未入力は destructive、任意の未入力は warning で見分ける。空では保存させない（閉じてから「未入力」に戻ると気づけない）
- 説明文は置かず placeholder で示した（DESIGN.md §Writing 原則4）
- 初心者マークは既製アセット `assets/beginner-mark.svg`（Noto Emoji U+1F530 / Apache 2.0）を使用。標章は手で描くと似て非なるものになる。公式は黄 `#f4ed19` + ティール `#22aa98` で緑ではなく、フラットUIに合わせて光沢3枚だけ除いた

**ホームのおすすめ投稿を投稿作成につないだ**
- チップ押下で `?idea=` を渡し、企画画面の入力欄に入れた状態でフローを1画面目から始める。自動送信はしない（文面を確かめてから送るかは本人に任せる）

**作業ログの自動生成が本番で回った**
- 8/24 分を launchd（23:00）が自動生成。修正した収集スクリプトの初回本番実行で、収集対象をワークスペースまで広げた効果も確認できた

### 直した不具合
- 動画エディタのヘッダー「まとめて画像生成」に onclick が無く、開くはずの一括生成パネルも呼び出し0件で到達不能だった。配線し、中身の用語も「スロット/区間」→「か所」に直した。件数は毎回いまの空き数から計算する（書き置きの数字は嘘になる）
- チュートリアルの「動画を編集してみる」がエディタへ直リンクしていた。エディタは動画ができたあとに開く画面で単独の入口が無く、ワイヤーの見本データがあるから開けてしまうだけだったので、素材ルート経由に直した
- 共有部品の `var()` 全25箇所に既定値が無く、`--color-destructive-border` は `settings_wire` にしか無いため他3ファイルでは border 宣言ごと無効になって丸印が消えていた。全箇所に既定値を書いた
- アイコンを `.ti-*` からインラインSVGに変更。字形宣言は各ファイル任せなので、宣言が無い画面ではチェックが出なかった
- 保存キーを v2 に上げ、`load()` は保存値を既定の型に流し込む形にした。旧版の形（progress 欄が無い）を読むと `st.progress[k]` で落ちていた
- `sendChatMsg` が最初の composer を拾う実装だったため、id を付けて明示的に狙うようにした

## 2026-08-24（月）

### やったこと

**TikTokのブランド色を茶からティールに変更（29ファイル）**
- 旧値 `sns-tiktok #854f0b` が `warning #ad6201`、`sns-tiktok-subtle #fef3c7` が `warning-border #ffe6c4` と近く、TikTokの行が「警告」に見えていた。TikTokのブランド色（黒／シアン `#25F4EE`／マゼンタ `#FE2C55`）に黄系は無く、他3つがブランド由来なのにTikTokだけ無関係な色だった
- 新値 `#0a6f66` / subtle `#e5fbf7`。色相174.7°で `info`(212°) から37.3°・`success`(135.7°) から39°離し、subtle地のコントラスト5.60:1（既存3色の4.79〜6.04の帯に収まる）／白文字時6.04:1
- ワイヤー27本 + `DESIGN.md` + `help_site/app/globals.css` を一括更新。トークン定義行だけを置換したので、`analysis_posts` のサムネのグラデーションや `analysis_visual` の銅メダル `#fef3c7` は残っている

**カードの中の説明文・使い方・見本を落とす**
- `settings_wire` の運用目的から進み具合の案内・使い方ピル4個・資料カードの説明・「書き方の例」を削除（11箇所）。空状態は「まだ書いていません」だけに。目標ダイアログの週1/3/5/7本の説明4個も削除（本数は数字で自明）
- SNS設定から説明文18箇所、`request_wire` の発注相談／出張撮影から POINT・選択肢・ステップの補足18箇所、`board.html` のギャラリー注記を削除。選択肢を見分ける一文（言葉づかいの口調例・撮影レベルの説明）とデータ・ラベル・状態表示は残した
- `DESIGN.md` の §Writing に原則4「カードの中に説明文と使い方を置かない」を追加。補助説明を「噛み砕いてよい（1〜2行）」→「カードの中には置かない」に変更し、Do's の「説明・ヒントのチップは primary-subtle + primary」を失効扱いに（記録・推し情報のチップだけオレンジで残す）
- `iine-ai-vault/concepts/no-in-card-help-text.md` を新規作成し `INDEX.md` のデザイン系に登録

**SNS設定に投稿ルール4項目を追加**
- 実アプリの投稿ルール5項目のうちワイヤーには「使ってほしくない言葉」しか無かったため、推奨ワード・具体ルール・投稿言語・撮影レベルを既存の言葉づかいと同じ表示／編集切替（`.pj`）で追加
- 4項目を毎回入れ直さずに済むよう、カードの手前に「ほかのSNSから写す」を設置。候補は一覧の連携済みの行から作り、現在のSNSと未連携は出さない。上書きなので実行前に変更内容を提示し、写すのはルール5項目だけ（出し方・本数の目標・承認は含めない）。`closeRowMenus` が `.iconbtn` だけを見ていたので `.act` 配下すべてに広げた

**設定画面のカードを2列等幅に揃える**
- 3列に2/1で混ぜる弁当組みがルール追加で8枚になり幅727/355・高さ4種に散っていたため2列に統一。`repeat(2,1fr)` では429/653に割れる（1fr の最小値が auto で min-content に押し広げられる）ので `minmax(0,1fr)` で厳密に等幅にし、高さの近いカードを隣に置く並び順にした
- 見出し8枚に丸チップのアイコンを追加（色は全部 primary）。`.pj`(16px/左右18px/罫線なし) と `.card`(20px/左右20px/罫線あり) でずれていた3点を `.pj` 側に寄せ、本文の左右余白も18pxに統一（`.bento` の中だけ）
- チップを持つ見出しは中央揃えに（`.pj-hd` が `align-items:flex-start` でタイトルがチップ中心より9px上にずれていた）

**作業ログの素材収集をワークスペース対応に**
- `collect-worklog.sh` の収集先を、メインリポジトリ1箇所から「メイン + ネストしたリポジトリ + その linked worktree」に拡大。名前と場所が作り直すたびに変わるので `.git` の有無で毎回探し、コミット取得は `--all` にした
- ネストしたリポジトリはメイン側で pathspec 除外して二重計上を防止。`.gstack` / `.superset` / `.DS_Store` は除外し、セクションは中身があるときだけ本体に足す

### 直した不具合
- 見通しカードが541pxに縮み、グラフに残る幅263pxに対し12ヶ月の月ラベルが nowrap で約360px必要だったため、`.bar-chart` が枠から溢れてタイルの下に潜り月が読めなかった。グラフとタイルを縦積みにしてグラフに全幅を渡し、タイルは3列に寝かせた
- 見出しの「週3本で続けた場合の目安」を削除（すぐ下の key タイル「1週間に出す本数 / 3本」と同じことを言っている）。`goalSave` の `fcPerWeek` への代入も外した
- `settings_wire` で `ti-chevron-left` が未宣言のため「SNS設定にもどる」の矢印がずっと出ていなかった。`ti-copy` と `ti-info-circle` は同梱フォントに glyph が無いので `clipboard-list` / `help` に差し替え、撮影レベル用に `ti-photo` を追加
- 説明文が消えて1行になった選択肢カードが上揃えで文字がアイコン中心より上にずれていたので、価格行を持たないカードは中央揃えに
- `daily-worklog.sh` が「（この日のコミットなし）」等の日本語文言を grep して判定していたため、素材の書式を変えた時点で毎日空のログを書く状態に静かに倒れていた。`collect-worklog.sh` が素材ゼロの日に `<!-- WORKLOG:NO-MATERIAL -->` を出し、それを見て打ち切る方式に変更

### 次にやること
- 撮影レベルは文章ではなく画像・動画の提案内容を変えるもので他のルールと役割が違う。いまは投稿ルールに並べたままなので置き場所の再検討（コードにコメントで申し送り済み）

## 2026-08-22（土）

※ この日ぶんは未コミット。次のコミットで確定する。

### やったこと

**「台本から」のアイコンを巻物に変更（全画面）**
- ビデオカメラ（`ti-video`）とキラキラ（`ti-sparkles`）で画面ごとに割れていたのを、巻物1種に統一
- 対象5ファイル: `flow_video_script` / `flow_video_material` / `flow_image_v3` / `flow_image_material` / `post_editor_image`
- ワイヤーに埋め込んだ Tabler サブセットフォントに巻物（`ti-script`）が入っていないため、24グリッド・`stroke-width:2`・round cap を合わせたインライン SVG で自作
- `DESIGN.md` に「アイコンは全部 Tabler、自作は台本の巻物だけ」を明記

**選択カードのアイコンを丸チップ化（全画面）**
- 作成方法カード（画像／動画／完成品）と台本・素材カードのアイコンを 64px の丸ベタ + アイコン32px に変更
- 選択中 = `primary(#fe7235)` 塗り + 白アイコン、未選択 = `border(#efe7df)` 塗り + `muted-foreground(#757575)` アイコン
- 対象8ファイル（上記5つ + `flow_image_v2` / `flow_video_v2` / `flow_manual`）
- 当初 `primary-deep(#d54101)` + グレー塗りで組んだが濃すぎたため上記に修正。`DESIGN.md` の「白は大字なら primary 上で許容」の記述に合わせた
- アイコンサイズが v2系だけ25pxだったのを64pxに統一
- `DESIGN.md` に丸チップの規定（色・サイズ・インラインstyleで上書きしない）を追記

**案カード（まとめて作る）の余白修正**
- タグ行の下パディングが `0` で点線区切りにくっついていたのを、点線の上下 12px（spacing `md`）の対称に変更
- 対象2ファイル: `flow_video_script` / `flow_image_v3`

### 見つけた不具合（未修正）

**案カードが表示されない（4ファイル）**
- `<script>` ブロックの先頭が `)();` から始まっており、ブロック全体が SyntaxError で実行されない
- そのため `BULK_SAMPLES` が未定義になり、「◯件の案を見る」を押しても案カードが出ない
- 該当: `flow_image_v3` / `flow_video_material` / `flow_manual` / `flow_image_material`
- `flow_video_script` だけは正常
- ブラウザのコンソールには出ない（パース時エラー）ため、`node --check` で特定
- 8/21 のコミット `2379205` で同種のエラーを1件直しているが、残り4ファイルが未対応だった

### 次にやること
- 上記 SyntaxError 4ファイルの修正
- 作業ログのスプレッドシート連携と毎日の自動生成

---

## 2026-08-21（金）

コミット4件: `b4d03bc` / `7e531c0` / `3348234` / `2379205`
規模: 47ファイル / +4,475行 / −1,898行

### 会議（10:58 岩上さん×いいねAI）で出た要望への対応

| 要望 | 対応 |
|---|---|
| ダッシュボードの「AIらしさ」を抑え色数を減らす | パレット全面刷新 |
| 画像・動画・PDF・Webリンクを1箇所に集約 | いいねドライブの2タブを1つの一覧に統合 |
| 投稿作成フローの「次へ」ボタンを廃止 | 選択した時点で次へ進む方式に変更 |
| 仮予約／本予約をフロー内で選択 | 予約ダイアログで選択可能に。完了メッセージも書き分け |
| SNSプレビューにセーフティゾーンの点線 | 動画（台本）フローのプレビューに追加 |

詳細は [[raw/mtg-2026-08-21-iwagami]]。

### やったこと

**デザイン基盤の刷新**
- メインカラー `#ef6108` → `#fe7235`、背景を生成り `#fcf5ef` に
- 情報・補助用の青 `#0077ff` を新設（押せる要素には使わない運用ルールつき）
- サイドバーを縦グラデーションに変更、選択中・ホバーの表現を再設計
- 画面間で食い違っていた色定義6件を統一。TikTokのブランド色が2種に割れていたのも統一
- グラフの配色を単一色相ランプに再確定
- フォントを Inter + Noto Sans JP の2段構成に（英数字のみ Inter）
- 全33ファイル中25ファイルで Webフォントが読み込まれていなかったため修正
- 文字サイズの下限を14pxに引き上げ（該当449箇所）。40〜50代の管理画面未経験者を想定した判断

**投稿作成フロー**
- 選択肢が1つだけの画面は選ぶだけで次に進む方式へ（「次へ」ボタンを非表示）
- 選んだ作成方法に応じて正しい画面へ分岐するよう修正（「動画を選んだのに画像の枚数を聞かれる」不整合を解消）
- 仮予約／本予約をダイアログ内で選択可能にし、完了メッセージを状態別に書き分け（本予約=「予約ができました」／仮予約=「準備ができました」）
- 予約の入口を1つに統一。旧日時ピッカーを使っていた5画面を新ダイアログに接続
- 動画2フローの完了表示を、通知のみからモーダル表示に変更
- 完成画面で予約が確定済みの場合は「投稿を予約する」を表示しないよう修正

**いいねドライブ**
- 「素材」「AI読み取り資料」の2タブを1つの一覧に統合。種別の違いは詳細パネルの操作で表現
- 新規フォルダ作成・URL追加・アップロードの各ダイアログを実装（従来は押しても反応しない状態）

**設定画面**
- 「言葉づかい」を運用目的から SNS設定へ移設。編集フォームの補足説明を整理
- SNS設定を「一覧 → アカウント詳細」のドリルイン構成に再編
- 運用目標と見通しの画面を新規追加（旧アプリの目標設定に相当する画面が未整備だったため）
- Facebook同時投稿の項目を Instagram 以外では非表示に

### 直した不具合
- 台本のシーン表が表示されない不具合（データと描画処理の欠落を復旧）
- スクリプトの構文エラー1件（コンソールに出ないため `node --check` で特定）
- 画像台本ルートの画面ラベルが「動画（台本）」と誤表示
- 承認済み画面から予約する際、予約種別を選ばずに確定してしまう問題
- 枚数選択画面が自動送りの対象外だった

### ドキュメント
- 8/21 会議録を `raw/` に記録（グループ外の議題は除外）
- `DESIGN.md` と設計ナレッジ10件を新パレット・新方針に追従

